var assert = require('chai').assert;
var luhnTest = require('../luhn');

describe("Luhn's test", function() {
    it("should test if input passes Luhn's test", function() {
        var number = "123456789";
        var result = luhnTest(number);
        assert.isTrue(result);
    });
    it("should test if input passes Luhn's test", function() {
        var number = "123456789";
        var result = luhnTest(number);
        assert.isTrue(result);
    });
    it("should test if input passes Luhn's test", function() {
        var number = "123456789";
        var result = luhnTest(number);
        assert.isTrue(result);
    });
    it("should test if input passes Luhn's test", function() {
        var number = "123456789";
        var result = luhnTest(number);
        assert.isTrue(result);
    });
    

})